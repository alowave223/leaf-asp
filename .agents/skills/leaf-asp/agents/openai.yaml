interface:
  display_name: "Leaf Asp"
  short_description: "Repo-specific patterns and workflows for leaf-asp"
  default_prompt: "Use the leaf-asp repo skill to follow existing architecture, testing, and workflow conventions."
policy:
  allow_implicit_invocation: true